function ExpertiseCard({ title, description }) {
    return (
      <div className="expertise-card">
  
        <h3>{title}</h3>
  
        <p>{description}</p>
  
      </div>
    );
  }
  
  export default ExpertiseCard;